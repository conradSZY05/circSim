#include <SFML/Graphics/RenderWindow.hpp>

class DropdownMenu 
{
    public:
        DropdownMenu();
        void draw(sf::RenderWindow &window);
        void getSelection();

    private:
        
};